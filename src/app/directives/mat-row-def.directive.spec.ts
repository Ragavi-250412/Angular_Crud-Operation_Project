import { MatRowDefDirective } from './mat-row-def.directive';

describe('MatRowDefDirective', () => {
  it('should create an instance', () => {
    const directive = new MatRowDefDirective();
    expect(directive).toBeTruthy();
  });
});
